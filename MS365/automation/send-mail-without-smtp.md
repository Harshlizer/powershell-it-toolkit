# Send Mail Without SMTP

## Goal

Send mail through Microsoft Graph with an application registration instead of relying on SMTP credentials.

## Required Placeholders

- `<TENANT_ID>`
- `<CLIENT_ID>`
- `<CLIENT_SECRET>`
